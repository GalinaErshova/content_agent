Проверь каждый фрагмент учебного материала: подтверждается ли он приведёнными к нему источниками.

{{items}}

Верни СТРОГО JSON-массив, по одному объекту на каждый фрагмент, без текста вокруг:
[{"id": 1, "supported": true, "issue": ""}]
Поле id должно совпадать с номером фрагмента. Поле issue — кратко, чего не хватает, если supported=false; иначе пустая строка.
